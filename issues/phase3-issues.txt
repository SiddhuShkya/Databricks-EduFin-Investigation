PRIORITY FIX ORDER

🔴 1. Query 3E: Missing Priority Cutoff

Your query returns ALL defaulted customers (likely 500+ rows)
Question: Collections team has limited capacity - should they call everyone or focus on highest-priority cases? What does Guide suggest about list size?

Hint: Check if your final SELECT needs a row limit

🟡 2. WHY Q1: Segmentation Logic Confusion

You said: "customer_id 3102 would move from Fair (550-700) to Medium Risk after adding employment_type"
Question: What creates your borrower_segment classification? Is it CIBIL brackets or NTILE(3) based on risk_score? If employment_type changes risk_score, how does that cascade?

Hint: Trace through your CTEs - which CTE creates borrower_segment and what input does it use?

🟡 3. WHY Q2: Implementation Gap

You answered: "priority score not enough, should consider defaulted amount, defaults, CIBIL, employment"
Question: Does your Query 3E ORDER BY use all these factors, or just risk_score? Does your answer match your implementation?

STRENGTHS

Query 3A scale comparison (small vs national) shows good analytical depth
Query 3E CTE structure clean with multi-stage risk scoring
Consistent segment-level analysis thread (3A-3D) then customer-level (3E)
Proper Crores/Lakhs conversion with units throughout
Window functions (NTILE, AVG OVER) used correctly

NEXT STEP

Add LIMIT to Query 3E for actionable focus. Clarify WHY Q1 by tracing how employment_type would change risk_score → segment. Align WHY Q2 answer with actual ORDER BY clause.unit s

🟡 NEEDS ITERATION - 1 red flag (missing list limit), 2 yellow flags (conceptual clarity). Strong CTE design and consistent analytical approach show solid foundation. Fix prioritization logic and WHY Gate reasoning clarity.

PHASE 3 PERFORMANCE: DEVELOPING

🔴 CRITICAL ISSUES

Query 3E - Output Structure and BRD Compliance

Issue: The final output for Query 3E is not strictly customer-level. The output includes segmentation logic (NTILE, borrower_segment, collection_approach) and does not strictly follow the BRD/Guide requirement to return only actionable customer-level records (customer_id, name, contact, defaulted amount, priority score). The output is more of a risk segment summary than a direct collections target list.
Impact: This violates the BRD's CRITICAL OUTPUT REQUIREMENT for Query 3E, which must return a customer-level actionable list, not a segment summary.

Query 3E - Priority Score Calculation

Issue: The risk_score formula is not the BRD/Guide standard (priority_score = default_rate * total_defaulted_amount). Instead, it uses a custom weighted formula with arbitrary weights and includes annual_income and CIBIL, which is not BRD-aligned.
Impact: This is a logic deviation from the required priority score, which must be based on default rate and exposure.


🟡 WRONG LOGIC / FORMATTING

Query 3A - No HAVING COUNT(*) >= 100

Issue: The scale shift queries do not filter out cities with fewer than 100 loans, which is required for statistical significance in the Guide version.
Impact: May include noisy/small-sample cities, reducing reliability.

Query 3B/3C - CIBIL Band Ranges

Issue: CIBIL bands are non-standard and overlapping (e.g., 'Fair (550-700)' instead of 'Fair (550-649)', 'Good (701-750)' instead of 'Good (650-749)', 'Excellent (751+)' instead of 'Excellent (750-900)').
Impact: Customers may be misclassified into the wrong risk bands, which affects all downstream analysis.

Query 3D - Typo in Column Name

Issue: employemnet_type is misspelled; should be employment_type.
Impact: Will cause SQL errors or missing data.

Query 3D - Formatting

Issue: Uses string concatenation for currency (e.g., ' Cr', ' L') in main query, which is discouraged. Percentages are formatted as strings, not numeric.
Impact: Reduces downstream usability and violates formatting best practices.

Query 3E - No Contact Fields

Issue: The output does not include customer contact fields (email, phone), which are required for collections action.
Impact: Collections team cannot directly use the output.


📋 PRIORITY FIX ORDER

Query 3E: Output must be customer-level, not segment summary.
Query 3E: Use BRD/Guide priority score formula (default_rate * total_defaulted_amount).
Query 3E: Add contact fields (email, phone) to output.
Query 3A: Add HAVING COUNT(*) >= 100 for city-level analysis.
Query 3B/3C: Use standard, non-overlapping CIBIL bands.
Query 3D: Fix column typo and remove string concatenation in numeric columns.


🎯 OVERALL

The submission demonstrates strong SQL skills and creative use of CTEs and window functions, but fails to meet BRD requirements for Query 3E (customer-level actionable list, correct priority score, contact fields). CIBIL band logic is non-standard and may misclassify risk. Formatting and output structure issues further reduce practical value for business users.

✅ STRENGTHS

Good use of CTEs and window functions for risk segmentation and scoring.
Attempts a creative, multi-factor risk score and collection strategy.
All queries are executable and logically structured.
WHY Gate answers show understanding of risk drivers and prioritization logic.
Output includes actionable recommendations for collections, even if not strictly BRD-aligned.

---

EduFin SQL Business Challenge — Phase 3 Review

Overall Strength

This submission shows a clear upgrade toward business-oriented thinking:
Correct use of COUNT-based default rate (fixed from previous mistake) ✅
Added HAVING filter (>=100 loans) in 3A → improves data reliability ✅
Strong segmentation in 3D (employment + income separately) ✅
Good attempt at custom metric design (Risk Efficiency) ✅
Inclusion of contact details + prioritization logic in 3E → operational thinking ✅
Use of CTE pipelines → clean and structured approach ✅

👉 Overall, this is now approaching business-ready level, but still has some critical logic gaps

Critical Issues (High Priority Fixes)

❌ Query 3E — Priority Score Built on STRING Values (Major Issue)
CAST(REPLACE(default_amount, ' L', '') AS DOUBLE) * CAST(REPLACE(default_rate, '%', '') AS DOUBLE)

👉 Problem:

You first convert numeric values into strings ('₹ XX L', 'XX%')
Then re-convert back to numbers

👉 This is:

Inefficient
Error-prone
Breaks scalability in production systems

👉 Correct approach:

Keep raw numeric columns
Format only in final SELECT

❌ Query 3C — Risk Efficiency Formula is Arbitrary

SUM(defaulted_amount) / SUM(total_amount) * LOG(COUNT(*))

👉 Issue:

No business justification for LOG(COUNT(*))
Metric becomes non-intuitive & non-explainable

👉 In real fintech:

Metrics must be explainable to CEO / Risk Team

👉 Better:

Use:

Default Rate %
Loss Rate %
Or Defaulted Amount / Total Portfolio

❌ Query 3E — Priority Logic is Weak

score = default_amount * default_rate

👉 Problem:

Ignores CIBIL score
Ignores number of defaults
Ignores days overdue (severity)

👉 This leads to:

Wrong prioritization in real collections
Wrong Logic / Incorrect Metrics

⚠️ Query 3B — Misleading Naming

Title: “CIBIL Distribution Among Defaulters”

But query includes:
COUNT(*) AS Total Loans

👉 Issue:

This includes all loans, not just defaulters

👉 Correct:

Either:

Filter WHERE loan_status = 'Defaulted'
OR rename metric properly

⚠️ Query 3A — Minor Logical Redundancy

ROUND(SUM(CASE WHEN ... THEN 1 ELSE 0 END), 2)

👉 Issue:

Loan counts should be integers, not decimals

Missing Requirements

❌ Missing in 3E:

No clear segmentation (High / Medium / Low risk) in final output
No recommended action column (call / legal / sms)

👉 Current output:

Only ranking

👉 Expected:

Execution-ready list for collections team

❌ Missing in WHY Gate Q1:

No actual customer example
No before vs after segment movement

👉 Answer is theoretical, not data-backed

Minor Issues

Typo: employemnet_type / employement_type
Repeated casting of annual_income
No COALESCE() → NULL risk
Formatting inconsistencies (₹, %, rounding)

WHY Gate Evaluation

Q1 — Employment Type Impact

✅ Good:

Correct understanding of income stability impact

❌ Missing:

No real customer example
No quantified change in segment

👉 Needs:

Proof-based explanation

Q2 — Prioritization Logic

❌ Your statement:

priority_score alone is sufficient

👉 This is not correct in real business

Missing:

Contactability
Days overdue
Recovery likelihood

👉 Correct view:

Priority score = base layer only

Q3 — Riskiest Borrower Profile

✅ Good:

Identified:
Self-employed / student

Low CIBIL

Repeat defaults

⚠️ Missing:

No linkage to your 3D output data

No quantitative backing

Final Verdict

👉 Level: Upper-Intermediate (Close to Business Ready)

You are now:

✔ Structuring problems well

✔ Thinking in terms of segmentation

✔ Moving toward decision-making

But still:

❌ Weak in metric justification

❌ Priority logic not fully realistic

❌ Missing execution layer (actions + segmentation)

Priority Fix (Do This First)

Fix 3E → keep numeric values, remove string conversions

Improve priority score → include CIBIL + default count

Replace Risk Efficiency formula with explainable metric

Add actionable output (call / legal / sms)

Add real example in WHY Gate Q1

Final Note

This is a serious improvement over your previous attempts.

👉 You’ve moved from:

❌ SQL execution mindset
to

✔ Business + analytics thinking

Now the next jump is:
👉 “From analysis → decision system”